export const routes = {
  index: `/`,
  login: `/login`,
  register: `/register`,
  flow: `/flow`,
  friends: `/friends`,

  api: {
    friends: `/api/friends`,
    removeFriends: `/api/friends/remove`,
    availableFriends: `/api/availableFriends`,

    login: "/api/login",
    getUser: "/api/getUser",
    register: "/api/register",
    logout: "/api/logout"
  }
};
